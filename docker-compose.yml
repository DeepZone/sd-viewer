services:
  backend:
    build: ./backend
    environment:
      # Ollama via your existing reverse proxy (SSL/TLS terminates there)
      OLLAMA_BASE_URL: "http://192.168.59.110:11434"
      OLLAMA_BASIC_USER: "noisens"
      OLLAMA_BASIC_PASS: "9hBn8xf%&smR*q"
      OLLAMA_MODEL: "qwen2.5:7b"

      DATA_DIR: "/data"
      MAX_UPLOAD_BYTES: "20000000"
    volumes:
      - ./data:/data
    ports:
      - "8088:8088"

  frontend:
    build: ./frontend
    environment:
      # Frontend talks to backend directly (no reverse proxy needed here)
      VITE_API_BASE: "http://192.168.59.110:8088"
    ports:
      - "8080:8080"
    depends_on:
      - backend
